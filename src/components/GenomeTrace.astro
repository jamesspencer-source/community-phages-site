---
interface Props {
  label?: string;
}

const { label } = Astro.props;
---

<div class="genome-trace" aria-label={label ?? undefined} aria-hidden={label ? undefined : 'true'}>
  <span></span>
  <span></span>
  <span></span>
  <span></span>
</div>
