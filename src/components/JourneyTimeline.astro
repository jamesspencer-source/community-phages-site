---
import { programWeeks } from '@data/site';

interface Props {
  compact?: boolean;
}

const { compact = false } = Astro.props;
const weeks = compact ? programWeeks.slice(0, 4) : programWeeks;
---

<div class:list={['method-sequence', compact && 'method-sequence--compact']}>
  {
    weeks.map((week) => (
      <article class="method-sequence-card">
        <div class="method-sequence-card__marker">
          <span>{String(Number(week.week)).padStart(2, '0')}</span>
        </div>
        <div>
          <h3>{week.title}</h3>
          <p>{week.short}</p>
          <div class="tag-row">
            {week.tags.map((tag) => (
              <span>{tag}</span>
            ))}
          </div>
        </div>
      </article>
    ))
  }
</div>
